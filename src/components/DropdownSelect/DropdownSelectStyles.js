import { createStyles } from '@material-ui/styles';

const styles = createStyles({
  container: {
    display: 'flex',
    flexWrap: 'wrap',
    minWidth: 200
  },
  formControl: {
    width: '100%'
  },
  disabled: {
    opacity: 0.5
  }
});

export default styles;
